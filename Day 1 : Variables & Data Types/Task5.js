// Task 5 : Declare a variable using let, assign it an initial value, reassign new value, and log both values to the console.

let pcProcessor = "Ryzen";
console.log(`what is first processor of pc is ${pcProcessor}`)
pcProcessor = "intel"
console.log(`Now new processor of pc is ${pcProcessor}`)